Run this command to make you have all the packages installed
```bash
pip install fastapi uvicorn psycopg2-binary
```


To execute the code, run this command in the terminal
```bash 
uvicorn main:app --reload
```
